# https://everything.curl.dev/ is a GREAT RESOURCE

--trace curl_trace.txt
--dump-header curl_headers.txt

# referrer URL -e
-referer /

# http://www.useragentstring.com/
# -A
--user-agent "Mozilla/5.0 (Windows; U; Windows NT 6.0; en-US) AppleWebKit/525.13 (KHTML, like Gecko) Chrome/0.2.149.27 Safari/525.13"

# headers -H
-header "Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8"
-header "Upgrade-Insecure-Requests: 1"
-header "Accept-Encoding: gzip, deflate, sdch"
-header "Accept-Language: en-US,en;q=0.8"
-header "Connection: Keep-Alive"
-header "Keep-Alive: timeout=5 max=100" # or use --keepalive-time / --no-keepalive

# follow redirects
--location  # -L

# --verbose

--trace-time
--trace-ascii

# ok if certificate validation fails
--insecure
